#include<stdio.h>
#include<string.h>
int main(){
    char str[100][100];
    strcpy(str[0], "eat");
    strcpy(str[1], "tea");
    strcpy(str[2], "tan");
    strcpy(str[3], "ate");
    strcpy(str[4], "nat");
    strcpy(str[5], "bat");

    for(int i = 0;i<strlen(str[][100]);i++){
        
    }
    return 0;
}